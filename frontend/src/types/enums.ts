/**
 * Loan status values from backend.
 */
export type LoanStatus = 'BORROWED' | 'RETURNED' | 'OVERDUE';

/**
 * User role values from backend.
 */
export type UserRole = 'ADMIN' | 'MEMBER';

/**
 * User status values from backend.
 */
export type UserStatus = 'ACTIVE' | 'BANNED' | 'INACTIVE';
